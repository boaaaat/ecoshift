# Interaction and camp fixes

- Shift-clicking carried armor now targets the armor slot before chest/inventory quick transfer. Actual Left Shift + storage click equipped Reed Sunwrap; Right Shift + hotbar click equipped Desert Cloak and returned the Sunwrap to the clicked slot with every inventory slot occupied. The real server snapshot retained both items, and DesertCloak was attached to the character. Source checkpoint e6e8f2c was pushed; no Roblox publishing occurred.
- Chest, gathering and ground-pickup prompts now display at eight studs without requiring line of sight. Server checks use the same prompt origin with a half-stud tolerance. Crafting stations retain their configured range, disable line of sight, and place their prompt at the same model pivot the crafting validator uses. A placed Workbench's prompt origin error was zero.
- Actual E input opened a chest behind an opaque wall at 6.54 studs. A reed gathering prompt at 6.44 studs, with an explicit ray confirming the intervening wall, completed its hold and awarded four ReedFiber in the real InventoryUpdate snapshot. The first disposable reed was placed in an unbound container; moving it into the normal streamed chunk exercised the actual prompt binding successfully.
- Server placement accepted Workbenches at X=12 and X=96 inside the camp, rejected X=108, and rejected X=99.1 because it snapped to X=102. Rejections returned OutsideCamp and retained the two unused Workbenches. Preview and server share the same circular boundary. Existing saved player builds are not deleted or relocated.
- A fresh Forest sample contained 1,674 resource models, 1,077 prop models and 18 landmarks with no footprint overlapping the 100-stud camp. The clearance helper rejected a four-stud-wide object at X=101, accepted it at X=103, and correctly handled rotated geometry and off-center pivots. This is a bounded generation sample, not a full-world scan of every seed.
- A subsequent Desert shift contained 1,497 resource models, 1,065 props and 20 landmarks with no overlapping camp footprints. Client ArtBiome attributes confirmed Desert content. The command-context BiomeService instance retained its separate default Forest value and was not used as evidence of the running biome.
- Generation keeps its original 80-stud sampling radius and spacing reservations while applying the new 100-stud footprint filter before publishing objects. Rejected candidates consume their saved ordinal; a focused check rejected the first candidate and assigned the following accepted candidate ordinal two. Rejected landmarks consume legacy chest-socket random draws without publishing chests. A current-source isolated check matched the next random draw after two normal chest attempts versus replay, with no replay chest attempts. Initial fixture socket names lacked the required underscore and exercised no sockets; only the corrected ChestSpawn_1/2/3 fixture is counted. Fresh enemy group placement also applies the footprint filter; existing actor movement and restored positions are not blocked by it.
- Both final Rojo builds and the diff check passed. The new clearance module synced without a Rojo restart. Play was stopped, removing temporary inventory, walls, nodes, chests, build fixtures and cloned modules.
- All gameplay fixtures used disposable Studio objects and inventory. No real saved-world records were modified. Live crew checks remain deferred, and the user handles Roblox publishing.

## Revival vitals and player map portraits

- Revival now sets Health to 30, Temperature (exposure) to zero, Hunger to 50 and Stamina (energy) to 50 before and after character loading. An isolated DeathService check using real StatsService and Humanoids passed from both +100 and -100 exposure with MaxHealth 200: all four values were exact, Humanoid health was 30, one simulated kit was consumed and death bookkeeping was cleared. Character loading and kit inventory were simulated; this was not a two-account revival session.
- Actual Studio headshot loading passed on both maps: approximately 18 pixels on the minimap and 20 pixels on the full map. An isolated two-player roster displayed the local heading at 0 degrees and a remote heading at 90 degrees, then independently updated to -90 and 180 degrees. The map canvas stayed north-up at zero rotation. The remote fixture had no character, exercising replicated position/heading fallback. Removing the fixture teammate removed both portraits. The command context could not access the fixture LocalScript's private global, so no streamed-character override check is claimed.
- The running server replicated the real player's changed heading. A focused MapTrackingService check verified live position/heading and cleared them for dead/loading players. Portrait images load asynchronously outside the render loop; distant character streaming does not remove server map telemetry.
- Play was stopped to remove all temporary fixtures. Both Rojo builds passed. No Roblox publishing occurred; real crew checks remain pending.

## Menu cursor and fallen-player portraits

- Shared MenuCursor registration now releases the cursor for inventory, chests, map, hand crafting, workbenches, recipe guide, settings, building selection, crew/ invites/member menus, world instruments/ballots and death results. It checks ancestor visibility and ScreenGui.Enabled, preserving ownership until every open menu closes. It uses Roblox's [GuiButton.Modal](https://create.roblox.com/docs/reference/engine/classes/GuiButton/Modal) camera integration plus a post-camera cursor override; existing first-person and Control shift-lock settings are retained.
- Actual Studio first-person checks passed for 12 panel types: MouseBehavior changed from LockCenter to Default with a visible cursor, and returned to LockCenter after closure. The ballot's normal refresh hid a manually toggled test panel; holding it visible during refresh verified its registration. Closing a chest while inventory remained open kept the cursor free. Actual Control input enabled shift-lock, which released for inventory and restored afterward.
- Dead map telemetry now follows DeathService's ragdoll position and orientation. A disposable server fixture verified a player with no character, a moved corpse, death-position fallback after the corpse was removed, and clearing while loading. A client fixture with two characterless players rendered all four portraits (self/teammate on both maps) red with loaded thumbnails, then restored white and rotated east when marked alive. This checks rendering and state changes, not a live two-account death/revival session.
- Portrait dimensions increased 20%: 24 pixels on the full map and a 21.6-pixel minimap target (approximately 21.34 measured at the current HUD scale). The local minimap continues at the corpse position while dead; dead bodies do not reveal new fog.
- Both final Rojo builds passed, the new cursor module synced without a restart, and Play was stopped to remove fixtures. No Roblox publishing occurred. Live crew checks remain pending.

## Shared expedition exploration

- TeamExplorationService now records exploration from living server character positions using the existing one-chunk reveal radius. Only discovered cells and their biome/region chart data replicate to clients. The map merges this chart with local geometry so distant discoveries survive streaming unloads; late map clients read the same existing chart. Dead/loading players cannot reveal cells.
- Fresh Studio boot published nine nearby cells with Forest region metadata. A simulated server teammate at X=1080 revealed nine additional cells while the observer stayed near spawn. The same simulated teammate marked dead revealed nothing. Both the existing map and a later cloned map client rendered the distant region after its temporary source geometry was destroyed. This exercised two map instances and a simulated teammate, not two real network clients.
- Actual WorldSnapshotService capture included all 18 shared cells and the distant region details. An isolated JSON plus WorldSnapshotCodec round trip preserved region data through restore and same-epoch initialization. A biome change retained discovery while replacing the biome and clearing old region labels; old-epoch metadata and duplicate saved cells were rejected. A nil exploration field restored an empty chart for legacy saves. Real multi-account saved-world resume remains pending.
- Server-generated metadata refreshes known cells as chunks generate. Exploration is included in the shared world snapshot, and a new world begins with an empty chart. The final finite-position and snapshot-validation-order checks were reviewed; both final Rojo builds and diff checks passed. Play was stopped to remove all fixtures. No Roblox publishing occurred.
